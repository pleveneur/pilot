# Pilot v0.2.17

## 🐛 Corrections

- **Une seule instance de Pilot** : relancer Pilot alors qu'une instance est
  déjà ouverte (iconifiée) ne démarre plus une deuxième instance bloquante.
  L'instance existante se restaure et prend le focus. Fin du conflit de port
  de l'accès à distance web quand deux instances tournaient en parallèle.
  *(issue #3)*
- **Jauge de contexte en temps réel** : le nombre de tokens et le contexte
  utilisé en bas de l'agent se mettent maintenant à jour **pendant** le
  streaming (y compris durant une longue réflexion), au plus une fois toutes
  les 3 s. Vous n'êtes plus surpris par une compaction automatique qui
  semblait arriver « à moins de 20 % » de contexte alors que l'affichage
  était resté figé. *(issue #2)*

---

_Mise à jour détectée automatiquement au démarrage de Pilot._